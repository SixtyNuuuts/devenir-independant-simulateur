{% extends 'base.html.twig' %}

{% block title %}FinancialItem{% endblock %}

{% block body %}
    <h1>FinancialItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ financial_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ financial_item.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ financial_item.value }}</td>
            </tr>
            <tr>
                <th>Nature</th>
                <td>{{ financial_item.nature }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ financial_item.type }}</td>
            </tr>
            <tr>
                <th>Attributes</th>
                <td>{{ financial_item.attributes ? financial_item.attributes|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_financial_item_index') }}">back to list</a>

    <a href="{{ path('app_financial_item_edit', {'id': financial_item.id}) }}">edit</a>

    {{ include('financial_item/_delete_form.html.twig') }}
{% endblock %}
